# Strengths
1. Lightweight possibility to store a history of changes
2. Possible to precisely travel through all the changes that have been made
# Weaknesses
1. Only suitable for text-files, not binaries, data files or images
2. Many ways to do the same thing (e.g. ``rebase`` to merge or ``merge`` to merge), may lead to confusion. Or overloaded functions like `checkout`.